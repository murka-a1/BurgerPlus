<Page x:Class="BurgerPlus.Page.PageUsers"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BurgerPlus.Page"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageUsers" IsVisibleChanged="Page_IsVisibleChanged">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="356*"/>
            <RowDefinition Height="93*"/>
        </Grid.RowDefinitions>
        <DataGrid x:Name="DtGridUsers" Grid.Row="0" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Логин" Binding="{Binding Login }" Width="25*"/>
                <DataGridTextColumn Header="Пароль" Binding="{Binding Password}" Width="25*"/>
                <DataGridTextColumn Header="Фамилия" Binding="{Binding LastName}" Width="30*"/>
                <DataGridTextColumn Header="Имя" Binding="{Binding FirstName}" Width="30*"/>
                <DataGridTextColumn Header="Отчество" Binding="{Binding Patronymic}" Width="45*"/>

                <DataGridTemplateColumn Width="auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Редактировать" Name="BtnEdit" Click="BtnEdit_Click" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Добавить" x:Name="BtnAdd" Click="BtnAdd_Click" Height="25" Width="111" Margin="10" />
            <Button Content="Удалить" x:Name="BtnDel" Click="BtnDel_Click" Height="25" Width="111" Margin="10" />
        </StackPanel>
    </Grid>
</Page>